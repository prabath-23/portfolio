Place your glTF model here so `ThreeScene.vue` can load it:

- File path: `frontend/public/models/developer-laptop.glb`
- URL at runtime: `/models/developer-laptop.glb`

Tip: Prefer a single `.glb` (binary glTF) so you don’t have to manage separate `.bin`/texture paths.

